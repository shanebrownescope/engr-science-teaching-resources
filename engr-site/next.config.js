/** @type {import('next').NextConfig} */
/** @type {import('next').NextConfig} */

const nextConfig = {
  // experimental: {
  //   runtime: "nodejs" // or 'nodejs' or 'edge'
  // }
};

module.exports = nextConfig;
